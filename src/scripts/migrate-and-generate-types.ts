// Purpose of this script is to bundle the migration run with the type generation in order to
// avoid having the src/types/index.ts file become stale in case a developer forgets to run
// the generate types npm script
